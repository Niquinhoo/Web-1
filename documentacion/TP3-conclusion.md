# TP3 Conclusion

TP3 queda funcionalmente cerrado.

Proyecto ya hizo salto real de persistencia mock a persistencia con SQLite. Productos, categorias, carrito, validacion de IDs y estructura base para usuarios ya trabajan sobre base de datos. No hay dependencia runtime de JSON, rutas principales siguen vivas, vistas no cambian para persona usuaria, y tabla `users` queda lista para Sprint 4 sin adelantar login ni hashing.

Veredicto:

- Codigo: `OK`
- Runtime: `OK`
- Consigna sprint: `OK`
- Riesgo actual: `BAJO`

Unica observacion real no bloqueante:

- Documentacion vieja todavia nombra `productModel.js`, JSON y columna `password`.

Conclusion final:

TP3 esta aprobado a nivel implementacion. Si quieren dejar entrega mas prolija, siguiente paso chico seria limpiar docs legacy para que reflejen estado final exacto de sprint.
