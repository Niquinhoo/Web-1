<nav class="navbar">
    <% const safeCartItemCount = Number.isFinite(Number(cartItemCount)) ? Number(cartItemCount) : 0; %>
    <div class="navbar-container">
        <a href="/home" class="navbar-logo">
            <span class="logo-icon">🍔</span>
            <span class="logo-text">Pediloo</span>
        </a>
        
        <%- include('../../molecules/home/search-bar') %>
        
        <div class="navbar-actions">
            <%- include('../../molecules/home/nav-item', { 
                link: '/cart', 
                icon: '🛒', 
                label: 'Carrito', 
                badge: String(safeCartItemCount)
            }) %>
            <%- include('../../molecules/home/nav-item', { 
                link: '/login', 
                icon: '👤', 
                label: 'Mi Cuenta' 
            }) %>
        </div>
    </div>
</nav>
